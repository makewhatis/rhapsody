# CLAUDE.md Maintenance Skill Implementation Plan

> **For agentic workers:** This plan is executed by a Rhapsody-dispatched agent with zero
> conversation context, inside its own forked git worktree — not by subagent-driven-development or
> executing-plans in an interactive session. Follow the tasks below in order; each is small enough
> to fit comfortably in the turn budget stated at the end of this document. End with the literal
> line `HANDOFF: in-review` once both tasks are done and the acceptance gate passes.

**Goal:** Add a repo-checked-in skill (`.claude/skills/claude-md-maintenance/SKILL.md`) documenting
how to detect and targeted-fix drift in `makewhatis/rhapsody`'s 25 nested `CLAUDE.md` files, plus a
matching `claude-md-maintenance` entry in Rhapsody's default agent-capabilities registry so a
dedicated Linear sweep ticket can opt into it.

**Architecture:** Two independent additions — a markdown skill file (no code, no tests in the
traditional sense) and one new `CapabilityDef` literal appended to
`crates/config/src/capabilities.rs::default_capabilities()`. Every existing test in that file
asserts against `default_capabilities()` dynamically (no hardcoded list, count, or order), so
appending a 10th entry does not require touching any existing test — confirmed by reading the full
`#[cfg(test)]` module before writing this plan.

**Tech Stack:** Rust (the `config` crate, `rhapsody-config`), Markdown (the skill file). No new
dependencies.

## Global Constraints

- The skill file documents an *inline-only* `Workflow` script — it must never instruct anyone to
  write a persisted `.claude/workflows/*.js` file (per the design spec's carried-over hard
  constraint from the original CLAUDE.md-generation methodology).
- Do not rename any existing capability's `name` field in `default_capabilities()` — those are
  stable identifiers used in `rhapsody:<name>` Linear labels and any already-seeded
  `~/.rhapsody/capabilities.yaml` on a user's machine (per `rhapsody-capabilities-sync`'s own
  documented rule).
- No `.github/workflows/*` changes — out of scope for this plan, and currently blocked by a missing
  `workflow` OAuth scope on this repo's push credentials (see PR #34/#35 history).

---

### Task 1: Add the `claude-md-maintenance` skill file

**Files:**
- Create: `.claude/skills/claude-md-maintenance/SKILL.md`

**Interfaces:**
- Consumes: nothing from Task 2 — this file only *references* the capability name
  `claude-md-maintenance` as a string; it does not depend on Task 2's Rust code existing first.
- Produces: nothing Task 2 depends on either. These two tasks can be done in either order; this
  plan lists Task 1 first only because it's the larger, more careful piece of writing.

- [ ] **Step 1: Create the directory and write the file**

Create `.claude/skills/claude-md-maintenance/SKILL.md` with exactly this content:

````markdown
---
name: claude-md-maintenance
description: Use when a Linear ticket's whole purpose is a CLAUDE.md maintenance sweep for makewhatis/rhapsody — detecting and fixing drift in the repo's nested CLAUDE.md files against the code they describe. Never invoke this as a side effect of an unrelated feature ticket; it's dedicated-sweep-ticket only.
---

# CLAUDE.md Maintenance Sweep

## Overview

`makewhatis/rhapsody` has 25 nested `CLAUDE.md` files (root + 24 subdirectories), generated by a
live-composed Claude Code `Workflow` run (PR #35). They are snapshots: as code changes underneath
them, individual files can go stale — a renamed function, a changed placeholder name, a directory
that no longer exists, a new crate with no file at all. This skill finds and fixes that drift with
*targeted* edits, not a full regeneration of the tree.

Run this only when the ticket that dispatched you exists specifically to run this sweep. If you
were dispatched for unrelated feature work and happen to notice a stale CLAUDE.md nearby, that's
out of scope for this skill — leave it and mention it in your handoff notes instead.

## Step 1 — scoped structural discovery

Find the newest commit that touched any existing `CLAUDE.md`, then look at everything that changed
since:

```bash
newest_commit="" newest_ts=0
for f in $(git ls-files '*CLAUDE.md'); do
  ts=$(git log -1 --format=%ct -- "$f")
  if [ "$ts" -gt "$newest_ts" ]; then
    newest_ts=$ts
    newest_commit=$(git log -1 --format=%H -- "$f")
  fi
done
git diff "$newest_commit"..HEAD --stat
```

Read that diff's file list for two things:

- **New directories that plausibly warrant a `CLAUDE.md`** — a new crate under `crates/`, a new
  top-level subsystem. Apply the same judgment as the original generation methodology's structure
  step: does this directory have enough non-obvious, non-derivable-from-a-glance behavior to be
  worth a file, at what depth, relative to its nearest existing ancestor `CLAUDE.md`?
- **Existing covered directories that were deleted or gutted** to the point their `CLAUDE.md` now
  describes something that no longer exists.

## Step 2 — per-file drift check

For every existing `CLAUDE.md`, diff its directory since that file's own last commit — not the
global newest one from Step 1:

```bash
for f in $(git ls-files '*CLAUDE.md'); do
  dir=$(dirname "$f")
  last=$(git log -1 --format=%H -- "$f")
  if ! git diff --quiet "$last"..HEAD -- "$dir"; then
    echo "DRIFTED: $f"
  fi
done
```

A file with no output here is skipped entirely for the rest of this sweep — that's what keeps this
"targeted updates," not regeneration. For each `DRIFTED` file, read the actual diff
(`git diff "$last"..HEAD -- "$dir"`) and judge whether it makes any claim in the `CLAUDE.md`
inaccurate, or just touches unrelated lines in that directory. Only files with a real inaccuracy
move on to Step 3.

## Step 3 — fix drift via an inline Workflow

For every file flagged by Step 1 (new/retired) or Step 2 (content drift), author and run a
Claude Code `Workflow` **inline**, via the tool's `script` parameter — **never** write a persisted
`.claude/workflows/*.js` file. Compose the script fresh in this same turn, every time.

The script must, in order:

1. **Draft depth-first.** Process flagged files parent-directory-first, then children — a child
   file's non-repetition check must see its parent's *already-fixed* content, not the stale
   version that was true before this sweep started.
2. **Verify independently.** For each drafted edit, a *separate* agent from the one that drafted it
   checks: no content repeated from an ancestor `CLAUDE.md`, under 200 lines, every claim concrete
   and non-derivable from a quick glance at the code (not restated obvious structure), and the
   exact header `# CLAUDE.md — <path>`.
3. **One bounded redraft** on a failed verification. If the redraft still fails, leave that file
   alone and note it in your final handoff summary for human review — do not merge a low-confidence
   edit.

## Step 4 — normal ticket flow

Commit your changes, open a PR against `makewhatis/rhapsody` the way any dispatched Rhapsody ticket
does, and end your final message with the literal line `HANDOFF: in-review`. There is nothing
sweep-specific about this step — it's the same mechanics every other ticket already uses.
````

- [ ] **Step 2: Verify the file's cross-references are accurate**

Confirm both referenced facts still hold at the commit you're working from:

```bash
find . -name CLAUDE.md -not -path './target/*' | wc -l
```

Expected: `25`. (If this number has changed since the plan was written, note the actual count in
your handoff summary — it doesn't block this task, since the skill's own Step 1 is what keeps that
number accurate going forward.)

```bash
gh pr view 35 --repo makewhatis/rhapsody --json state,mergedAt
```

Expected: `"state": "MERGED"`, confirming the PR #35 reference in the skill file's Overview section
is accurate.

- [ ] **Step 3: Commit**

```bash
git add .claude/skills/claude-md-maintenance/SKILL.md
git commit -m "docs: add the claude-md-maintenance skill"
```

---

### Task 2: Register the `claude-md-maintenance` capability

**Files:**
- Modify: `crates/config/src/capabilities.rs:36-92` (the `default_capabilities()` function body)
- Test: `crates/config/src/capabilities.rs` (inline `#[cfg(test)]` module, same file)

**Interfaces:**
- Consumes: nothing from Task 1.
- Produces: a `CapabilityDef { name: "claude-md-maintenance", .. }` entry that
  `default_capabilities()` returns as its 10th element. No other crate's code needs to change to
  pick this up — every consumer found in the workspace (`httpapi`, `rhapsodyd`, `orchestrator`)
  reads the whole `Vec<CapabilityDef>` generically; none hardcodes a length or specific names.

- [ ] **Step 1: Write the failing test**

Add this test to the existing `#[cfg(test)] mod tests` block in
`crates/config/src/capabilities.rs` (place it near the other `default_capabilities()`-only tests,
e.g. right after `capability_def_tolerates_partial_entries`):

```rust
#[test]
fn default_capabilities_includes_claude_md_maintenance() {
    let found = default_capabilities()
        .into_iter()
        .find(|c| c.name == "claude-md-maintenance")
        .expect("claude-md-maintenance capability present");
    assert_eq!(found.label, "CLAUDE.md Maintenance Sweep");
    assert!(found.instruction.contains("claude-md-maintenance/SKILL.md"));
}
```

- [ ] **Step 2: Run test to verify it fails**

Run: `cargo test -p rhapsody-config default_capabilities_includes_claude_md_maintenance`
Expected: FAIL — `claude-md-maintenance capability present` panic, since the entry doesn't exist yet.

- [ ] **Step 3: Add the capability entry**

In `crates/config/src/capabilities.rs`, inside `default_capabilities()`'s `vec![...]`, add this as
the final element (after the existing `second-opinion` entry, before the closing `]`):

```rust
        CapabilityDef {
            name: "claude-md-maintenance".to_string(),
            label: "CLAUDE.md Maintenance Sweep".to_string(),
            description: "Detect and fix drift in this repo's nested CLAUDE.md files".to_string(),
            instruction: "Read and follow .claude/skills/claude-md-maintenance/SKILL.md in this repo verbatim — it documents the full drift-detection and targeted-fix process for this ticket.".to_string(),
        },
```

- [ ] **Step 4: Run test to verify it passes**

Run: `cargo test -p rhapsody-config default_capabilities_includes_claude_md_maintenance`
Expected: PASS

- [ ] **Step 5: Run the full crate test suite**

Run: `cargo test -p rhapsody-config`
Expected: PASS — all existing tests still pass unmodified, since every one of them asserts against
`default_capabilities()` dynamically rather than a hardcoded list (verified by reading the test
module before writing this plan; nothing else in this crate, or in `httpapi`/`rhapsodyd`/
`orchestrator`, needs a change).

- [ ] **Step 6: Commit**

```bash
git add crates/config/src/capabilities.rs
git commit -m "feat(config): register the claude-md-maintenance capability"
```

---

## Self-Review

**Spec coverage:** The spec's Architecture section calls for exactly two artifacts — the SKILL.md
(Task 1) and the capabilities.yaml default entry (Task 2). The spec's "Process" section (4 steps)
is fully reproduced inside the SKILL.md file itself in Task 1, Step 1. The spec's own Testing
section (the future dogfood run) is explicitly out of scope for this plan — it's tracked
separately.

**Placeholder scan:** No TBD/TODO; every step has literal file content or an exact runnable
command with a stated expected result.

**Type consistency:** `CapabilityDef`'s four fields (`name`, `label`, `description`, `instruction`)
match the struct definition read directly from `crates/config/src/capabilities.rs:12-22` before
writing this plan — all four are `String`, matching the `.to_string()` calls in Task 2 Step 3.

## Acceptance gate, turn budget, and do-not-touch list

*(For the Linear ticket description — copy this section verbatim.)*

**Turn budget:** `agent.max_turns` default (20). This plan is two small, independent tasks — one
markdown file and one ~6-line Rust struct literal plus one new test — comfortably inside that
budget with room to spare.

**Acceptance gate (run both, in order, from the repo root):**

```bash
cargo test -p rhapsody-config
```
Expected: all tests pass, including the new `default_capabilities_includes_claude_md_maintenance`.

```bash
cargo fmt --all --check && cargo clippy --workspace --all-targets -- -D warnings
```
Expected: exits 0, no output (this is `make lint`; run it even though only one crate changed, since
it's the repo's real merge gate).

**Do not touch:**
- Any file outside `.claude/skills/claude-md-maintenance/SKILL.md` and
  `crates/config/src/capabilities.rs`.
- Any existing `CapabilityDef`'s `name` field in `default_capabilities()` — append only, never
  rename or reorder existing entries.
- `.github/workflows/*` — out of scope, separately blocked on an OAuth scope fix.
- `harness/fixtures/*` — this change has no effect on the golden-fixture pipeline; if any fixture
  test fails, that's a sign something unrelated broke and should be investigated, not silenced.
- Any of the other 24 `CLAUDE.md` files or the root `CLAUDE.md` — this ticket adds the *mechanism*
  for fixing drift, it does not itself perform a drift-fixing sweep.
