// Test-only static asset: lets the SPA-handler tests exercise the "serve an existing embedded
// file directly (not the index fallback)" branch of spa_response, with a real (non-HTML) MIME type.
export const marker = "rhapsody-test-asset";
