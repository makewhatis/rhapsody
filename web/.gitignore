node_modules
dist
# Wails generates JS/TS bridge bindings into the frontend dir on `wails build`; the app
# calls window.go.* directly via src/lib/bindings.ts, so the generated set is not committed.
wailsjs
*.local
.vite
*.tsbuildinfo
vite.config.d.ts
vitest.config.d.ts
# Playwright (the STUDIO-1023 header layout spec): its run output and browser cache are local.
test-results
playwright-report
.playwright
